(function () {
  if (window.mermaid) {
    // securityLevel: 'strict' (default); gunakan 'loose' bila perlu diagram dari HTML eksternal
    window.mermaid.initialize({ startOnLoad: true, securityLevel: "strict" });
  }
})();
